# Optional: pre-load your Anthropic API key so users don't need to enter it manually.
# Copy this file to .env and fill in your key.
# WARNING: Never commit your .env file — it's in .gitignore.
VITE_ANTHROPIC_API_KEY=sk-ant-api03-...
